-> Node hardware failure, -> Master node 
Master node was done, We are using val key redis 
In profile store which has very high RPS they observed the pattern that lot of requests are same only from multiple upstreams within same interval of time
So they initailised a worker pool on container main and batching the requests where they are keeping unique ones and passing the response to 
the same request properties using channels through worker pool pattern
So here basically one mistake was they are using base background context which doesn thav deadline, And val/key also during this hardware faliures
due to one bug it entered into infinite loop with isRetry function .isRetryable(resp.Error(), c
So gouritnes started getting stuct
In MultiGet because OF TCP Protocal its not caused any issues as requests timedout
But for Get goroutine mutext contention happend
And they are using single flight pattern 


handler goroutine
  → CacheRedisGet(ctx, key)
    → batcher.Get(ctx, key)
      → enqueue to b.pending
      → select {           ← line 143, goroutines stuck here
          case result := <-resultCh:     // path 1: flush sends result
                 → return result.val, result.err
          case <-ctx.Done():             // path 2: context cancellation
                 → return
        }


        Valkey 

        if  c.retry && allReadOnly(multi) {         // GET is read-only → true
    if c.isRetryable(resp.Error(), ctx) {   // non-ValkeyError + ctx.Err()==nil → true
        shouldRetry := c.retryHandler.WaitOrSkipRetry(ctx, attempts, ...)
        if shouldRetry {                    // no deadline → ALWAYS true
            attempts++
            goto retry                      // INFINITE LOOP
        }
    }
}

Node goes unavailable
     │
     ▼
pipeline.Exec(b.ctx)
     │
     ▼
CustomTimeoutHook: b.ctx has no deadline → replaces with context.Background()
     │
     ▼
pipe.syncDoMulti: conn.SetDeadline(200ms) → timeout fires ✓
     │
     ▼
singleClient.DoMulti: isRetryable? YES (ctx.Err()=nil) → WaitOrSkipRetry? YES (no deadline)
 
     └────────────────  goto retry, infinte loop ◄─────────────────────────────┘

Meanwhile:
  - run() blocked (flush is synchronous)
  - New requests enqueue, wait on resultCh → stuck
  - flushCh signals dropped (buffer=1, based on timer window/ pending request threshold breach)

<img width="873" height="420" alt="565342136-a66566af-6fde-4015-8141-2160d2f51b52" src="https://github.com/user-attachments/assets/3837f2d8-51c2-484c-8d8f-fa3570ae736f" />
<img width="640" height="1048" alt="564872421-81665b41-5435-4f14-bef4-e657140bb5d9" src="https://github.com/user-attachments/assets/e5d9a177-4f9b-4566-9846-e8c91fda02f0" />

